// Package integration holds end-to-end tests that drive the daemon
// against a headless Sway compositor. Each test file is gated behind
// the `integration` build tag so plain `go test ./...` stays fast.
package integration
